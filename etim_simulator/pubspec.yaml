name: etim_simulator
description: ETIM Simulator for Live Bus Tracking
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^13.0.2
  permission_handler: ^11.3.1
  provider: ^6.1.2
  firebase_core: ^3.1.1
  cloud_firestore: ^5.0.2

flutter:
  uses-material-design: true
